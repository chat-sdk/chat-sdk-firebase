{
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint"
    ]
  },
  "database": {
	  "rules": "chat-sdk-rules.json"
  },
  "storage": {
	  "rules": "chat-sdk-storage-rules.json"
  }
}

